export type FoodToCart = {
    message: string,
    productName: string,
    quantiry: number
}

export type Food = {
  id: string,
  name: string,
  price: number,
  slug: string,
  description: string,
  imageUrl: string
}